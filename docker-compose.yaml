services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: one_click_shield_backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: one_click_shield_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
